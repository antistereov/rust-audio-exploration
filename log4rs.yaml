refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
  file:
    kind: file
    path: "log/output.log"
    encoder:
      pattern: "{d} - {m}{n}"

root:
  level: warn
  appenders:
    - stdout
    - file

loggers:
  app:
    level: debug
